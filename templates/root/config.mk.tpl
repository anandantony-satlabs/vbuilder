# {{PROJECT}} — build configuration (single source for tool paths)
# Sourced by the top-level Makefile and scripts.

VERILATOR ?= verilator
PYTHON    ?= python3
UVM_HOME  ?= /usr/local/share/uvm

# Project identity
PROJECT   := {{PROJECT}}
MODULE    := {{MODULE}}
