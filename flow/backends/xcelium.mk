# ============================================================================
# vbuilder DV Flow — backend: xcelium (STUB)
# ============================================================================
# Cadence Xcelium (irun/xrun) backend. NOT YET IMPLEMENTED.
# Implement the five contract macros (COMPILE/RUN/COV/WAVE/LINT _CMD)
# per the contract in flow.mk. Xcelium uses `xrun -uvm` with `+UVM_TESTNAME`.
# ============================================================================

$(error [flow] xcelium backend not yet implemented — \
see $(FLOW_DIR)/backends/verilator.mk for the contract.)
