# ============================================================================
# vbuilder DV Flow — backend: questa (STUB)
# ============================================================================
# Mentor Questa (ModelSim) backend. NOT YET IMPLEMENTED.
# Implement the five contract macros (COMPILE/RUN/COV/WAVE/LINT _CMD)
# per the contract in flow.mk. Questa uses `vlog`/`vsim` with `+UVM_TESTNAME`.
# ============================================================================

$(error [flow] questa backend not yet implemented — \
see $(FLOW_DIR)/backends/verilator.mk for the contract.)
