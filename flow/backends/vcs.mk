# ============================================================================
# vbuilder DV Flow — backend: vcs (STUB)
# ============================================================================
# Synopsys VCS backend. NOT YET IMPLEMENTED.
# To implement, supply the five contract macros (see verilator.mk):
#   COMPILE_CMD, RUN_CMD, COV_CMD, WAVE_CMD, LINT_CMD
# conforming to the SV_SRCS / TESTS / TB_TOP / DUT contract variables.
# ============================================================================

$(error [flow] vcs backend not yet implemented — \
see $(FLOW_DIR)/backends/verilator.mk for the contract. \
Contributions welcome; the contract is documented in flow.mk.)
