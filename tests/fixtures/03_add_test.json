{
  "name": "add_test_autowires_four_points",
  "prompt": "Add an error_injection test (auto-wires pkg + filelist + Makefile + run_all)",
  "script": {
    "tool": "vbuilder",
    "args": {
      "action": "add_test",
      "projectDir": "/tmp/vb-fix-init",
      "test": "error_injection_test",
      "desc": "injects correctable errors"
    },
    "text": "test added"
  },
  "expect": {
    "alive": true,
    "outputContains": "dv/Makefile (TESTS)"
  }
}
